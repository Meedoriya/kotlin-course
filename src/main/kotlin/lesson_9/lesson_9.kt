package lesson_9

fun main() {

    val upsilon = Upsilon(50)

    upsilon.prepareForLanding()
    upsilon.prepareForTakeOff()
    upsilon.reloadGuns()
    upsilon.runDiagnostic()
    upsilon.startEngine()
    upsilon.startShooting()
}